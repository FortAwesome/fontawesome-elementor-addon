#!/bin/bash

set -e

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
PLUGIN_DIR="$(realpath "$DIR/../plugin")"
DIST_DIR="$PLUGIN_DIR/wp-dist"

( cd "$DIST_DIR" && zip -r "$PLUGIN_DIR/fontawesome-elementor-addon.zip" . )
